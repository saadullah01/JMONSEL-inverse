README
1. The fine tune model is saved as a checkpoint in the fine tune model folder. It can loaded as weights in the AUTOMAP/models. The models
can be downloaded from https://github.com/MattRosenLab/AUTOMAP
2. The test set is included in the inferred_test_data_images folder and can be used with AUTOMAP to test the model.  